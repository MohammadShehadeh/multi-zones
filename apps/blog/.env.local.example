# In production, set this to the publicly accessible URL of this zone
# e.g. https://blog.example.internal or https://example.com (if behind a gateway with rewrites)
NEXT_PUBLIC_ZONE_URL=http://localhost:3002
